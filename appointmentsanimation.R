trace(utils:::unpackPkgZip, edit=TRUE)
install.packages("animation")
install.packages("imager")
library("imager")

setwd("C:/Users/659163/Desktop")

setwd("C:/cygwin64/home/659163/GitHub/FederalJudiciary.github.io")

wigga <- c("./PresAppointmentMap1.png","PresAppointmentMap2.png","PresAppointmentMap3.png")


  for (i in 1:10) plot(runif(10), ylim = 0:1)
})


library(imager)
im<-load.image(wigga[1])
plot(im,axes=FALSE)

install.FFmpeg(page_with_download_url = "http://ffmpeg.zeranoe.com/builds/",
               ...)

runif(10)

library("png")
plot.new()
pp <- readPNG(wigga[1])
rasterImage(pp,0,0,10,1)
?rasterImage
